# Camera.setPosition, so a first-person ragdoll camera can sit at the head.
# Forge's ComputeCameraAngles event exposes yaw, pitch and roll but not position.
public net.minecraft.client.Camera m_90581_(Lnet/minecraft/world/phys/Vec3;)V # setPosition

# Explosion.radius, so blast strength and reach can scale the launch by distance.
# Reading damage instead does not work: a creative player takes none, and TNT damage
# falls off so fast that only someone standing inside it clears any threshold.
public net.minecraft.world.level.Explosion f_46017_ # radius
